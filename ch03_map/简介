数组与切片：它们代表一组连续存储的同构类型元素集合。不同的是，数组的长度是确定的，而切片，我们可以理解为一种“动态数组”，它的长度在运行时是可变的。
复合数据类型：原生map类型的实现机制是怎样的？
    什么是 map 类型？
        map 是 Go 语言提供的一种抽象数据类型，它表示一组无序的键值对。
            map[key_type]value_type
            map[string]string // key与value元素的类型相同
            map[int]string // key与value元素的类型不同
        map类型可以让你将一个值（Value）唯一关联到一个特定的键（Key）上，可以用于实现特定键值的快速查找与更新
        如果两个 map 类型的 key 元素类型相同，value 元素类型也相同，那么我们可以说它们是同一个 map 类型，否则就是不同的 map 类型。
        go语言中，map类型的key的类型必须支持“==”和“!=”两种比较操作符。因为需要保证key的唯一性。

        注意：函数类型、map 类型自身，以及切片类型是不能作为 map 的 key 类型的。
    map 变量的声明和初始化
        声明一个map变量：
            var m map[string]int // 一个map[string]int类型的变量，因为没有显式地赋予 map 变量初值，map 类型变量的默认值为 nil

            slice是零值可用的，初值为零值nil的切片类型变量可以借助内置的append函数进行操作。但 map 类型，因为它内部实现的复杂性，无法“零值可用”。
            所以，如果我们对处于零值状态的 map 变量直接进行操作，就会导致运行时异常（panic），从而导致程序进程异常退出。
            var m map[string]int // m = nil
            m["key"] = 1 // 发生运行时异常：panic: assignment to entry in nil map

        和切片一样，为 map 类型变量显式赋值有两种方式：一种是使用复合字面值；另外一种是使用 make 这个预声明的内置函数。
            使用复合字面值初始化 map 类型变量。
            m := map[int]string{}
            
            使用 make 为 map 类型变量进行显式初始化。
            m1_1 := make(map[int]string)    // 未指定初始容量
            m2_1 := make(map[int]string, 8) // 指定初始容量为8

            m1_1[1] = "val1"
            m2_1[1] = "val2"
            fmt.Println(m1_1, m2_1)

            //获取map键值对数量
            fmt.Println(len(m1_1))

            注意:我们不能对 map 类型变量调用 cap，来获取当前容量，这是 map 类型与切片类型的一个不同点。

            判断key是否存在于map中
            m := make(map[string]int)
            v, ok := m["key1"]
            if !ok {
            // "key1"不在map中
            }
            // "key1"在map中，v将被赋予"key1"键对应的value
        map注意事项：
            不要依赖 map 的元素遍历顺序；
            map 不是线程安全的，不支持并发读写；
            不要尝试获取 map 中元素（value）的地址。